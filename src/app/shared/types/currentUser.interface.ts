export interface CurrentUserInterface {
    email: string,
    token: string,
    username: string,
    bio: string | null,
    image: string | null,
    createdAt: string | null,
    updatedAt: string | null
}
